import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container:{
    flexDirection: 'row',
    alignItems: 'center'
  },
  textStatus:{
    fontSize: 15,
    fontFamily: 'OpenSans-Bold',
    color: '#37474F'
  },
});