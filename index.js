import {AppRegistry} from 'react-native';
import ContainerNavigation from './src/navigation/ContainerNavigation';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => ContainerNavigation);
